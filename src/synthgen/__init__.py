"""blender-synthgen-mcp — procedural 3D synthetic-data toolkit for Blender.

Subpackages:
  extract    — run-in-Blender / hython node-schema extractors
  schema     — query the extracted schemas (grounding, not model memory)
  scenegraph — lazy pull-based view over the live scene (Phase 2)
"""

__version__ = "0.0.1"
